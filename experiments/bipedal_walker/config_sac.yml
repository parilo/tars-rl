framework: tensorflow

env:
  name: BipedalWalker-v2
  is_gym: True

  obs_shapes: [[24]]
  obs_dtypes: ["float32"]
  obs_is_image: False
  history_length: [3]

  action_size: 4
  frame_skip: 8
  step_limit: 0
  agent_buffer_size: 2010
  reward_scale: 0.1
  reinit_random_action_every: 5
  log_every_n_steps: 1000
  max_episode_length: 2010

server:
  seed: 42
  num_clients: 16
  experience_replay_buffer_size: 5000000
  use_prioritized_buffer: false
  train_every_nth: 1.
  start_learning_after: 5000
  target_critic_update_period: 1
  target_actor_update_period: 1
  show_stats_period: 100
  save_model_period: 25000
  client_start_port: 10977
  logdir: "logs/bipedal_walker_sac_asd"
  #load_checkpoint: ""

algo_name: "sac"

algorithm:
  n_step: 1
  gamma: 0.99
  actor_grad_val_clip: 1.0
#  actor_grad_norm_clip: None
#  critic_grad_val_clip: None
#  critic_grad_norm_clip: None
  action_squash_func: "tanh"
  reward_scale: 10.
  mu_and_sig_reg: 0.00003
  target_critic_update_rate: 1.0

actor:
  nn_arch:

    - input: 0
      name: "output"
      layers:
        - type: "Flatten"

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 8  # number of actions x 2 for mu and sigma
            activation: "linear"
            kernel_initializer:
              class: "RandomUniform"
              module: "tensorflow.python.keras.initializers"
              args:
                minval: -0.003
                maxval: 0.003
            bias_initializer:
              class: "RandomUniform"
              module: "tensorflow.python.keras.initializers"
              args:
                minval: -0.003
                maxval: 0.003

critic_v:
  nn_arch:

    - input: 0
      name: "output"
      layers:
        - type: Flatten

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 1
            activation: "linear"
            kernel_initializer:
              class: "RandomUniform"
              module: "tensorflow.python.keras.initializers"
              args:
                minval: -0.003
                maxval: 0.003
            bias_initializer:
              class: "RandomUniform"
              module: "tensorflow.python.keras.initializers"
              args:
                minval: -0.003
                maxval: 0.003

critic_q:
  have_action_input: true
  nn_arch:

    - input: 0
      name: "flatten-obs"
      layers:
        - type: "Flatten"

    - input: ["flatten-obs", "action"]
      name: "output"
      layers:

        - type: "Concatenate"
          args:
            axis: 1

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 128
            activation: "relu"

        - type: "Dense"
          args:
            units: 1
            activation: "linear"
            kernel_initializer:
              class: "RandomUniform"
              module: "tensorflow.python.keras.initializers"
              args:
                minval: -0.003
                maxval: 0.003
            bias_initializer:
              class: "RandomUniform"
              module: "tensorflow.python.keras.initializers"
              args:
                minval: -0.003
                maxval: 0.003

actor_optim:
  module: tensorflow.train
  class: AdamOptimizer
  schedule:
    - limit: 0  # of train ops
      lr: 0.0001

critic_optim:
  module: tensorflow.train
  class: AdamOptimizer
  schedule:
    - limit: 0  # of train ops
      lr: 0.0001

training:
  schedule:
    - limit: 0  # of train ops
      batch_size: 2560

agents:
  - algorithm_id: 0
    agents:
      - agents_count: 1
        visualize: True
        exploration:
          built_in_algo: True
          validation: True

      - agents_count: 1
        visualize: False
        exploration:
          built_in_algo: True
          validation: True

      - agents_count: 1
        visualize: True
        exploration:
          built_in_algo: True
          validation: False

      - agents_count: 4
        visualize: False
        exploration:
          built_in_algo: True
          validation: False
